{
  "name": "react-css-transition-hook",
  "author": "Markus Ast",
  "version": "3.0.0-alpha.1",
  "license": "MIT",
  "description": "Minimal, zero-dependency React hook to CSS class name based transitions.",
  "keywords": [
    "react",
    "hook",
    "react hook",
    "transition",
    "CSS"
  ],
  "main": "dist/useTransition.cjs.js",
  "typings": "dist/useTransition.d.ts",
  "module": "dist/useTransition.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "rimraf dist && node build.js && tsc --emitDeclarationOnly",
    "prepare": "npm run build",
    "type-check": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": ">=16"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/react": "^17.0.0",
    "esbuild": "^0.13.15",
    "react": "^17.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  },
  "prettier": {
    "printWidth": 100
  }
}
